name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (${{ matrix.os }}, node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node: ["22.18", "24"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Unit and integration tests
        run: npm test

      - name: Baa's own test blocks
        run: node src/cli/index.ts test tests/programs examples/apps examples/native/notepad/tests

      - name: Run every example
        shell: bash
        run: |
          for file in examples/*.baa; do
            echo "--- $file"
            node src/cli/index.ts run "$file" > /dev/null
          done

  native:
    name: native runtime (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Linux proves the runtime is not secretly Windows-only; Windows is the
        # only place the window backend exists, so it is the only place the
        # application build and the window tests can run.
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "22.18"
          cache: npm

      - run: npm ci

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust

      - name: The generated diagnostic catalogue is current
        run: node tools/gen-native-codes.ts --check

      - name: Runtime unit tests
        run: cargo test --manifest-path rust/Cargo.toml

      - name: Build the runtime
        run: cargo build --release --manifest-path rust/Cargo.toml

      - name: Conformance suite, on the native runtime
        run: node tools/native-conformance.ts --verbose

      - name: Drift guards, the bundler and the runtime
        run: node --test tests/native.test.ts

      - name: An application builds and carries its program
        if: runner.os == 'Windows'
        working-directory: examples/native/calculator
        run: node ../../../src/cli/index.ts app build

      - name: The stated test count is true
        # Here rather than in `quality`: this is the only job with the native
        # runtime built, and without it twelve tests do not run and the total
        # is short by exactly that much.
        run: node tools/check-test-count.ts

      - name: Clippy
        run: cargo clippy --manifest-path rust/Cargo.toml --all-targets -- -D warnings

  extension:
    name: vs code extension
    # Windows on purpose. `npm install -g` writes `baa.cmd` here, which Node
    # refuses to spawn without a shell, and an extension that cannot start its
    # own server on the platform most people use is the failure worth catching.
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - run: npm ci

      - name: Build the server the extension will run
        run: npm run build

      # The Windows shim-derivation is unit-tested in `tests/vscode.test.ts`
      # against a fake filesystem, because a hosted runner does not lay out a
      # global npm install the way a user's machine does: asserting it here
      # would assert something untrue of this machine rather than catch a
      # regression. What this job covers is the client, given a server.

      - name: Compile the client
        working-directory: editors/vscode
        run: |
          npm ci
          npm run compile

      - name: How the client finds the server
        working-directory: editors/vscode
        run: npm test

      - name: The extension starts the server and reports diagnostics
        working-directory: editors/vscode
        shell: bash
        env:
          BAA_SERVER_PATH: ${{ github.workspace }}/dist/cli/index.js
        run: npm run test:vscode

      - name: Package the VSIX
        working-directory: editors/vscode
        run: npm run package

      - uses: actions/upload-artifact@v4
        with:
          name: baa-lang-vsix
          path: editors/vscode/baa-lang.vsix
          if-no-files-found: error

  quality:
    name: format, lint and docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - run: npm ci

      - name: Formatter is a fixed point
        run: node src/cli/index.ts fmt --check examples tests/programs

      - name: Linter is clean
        run: node src/cli/index.ts lint --deny-warnings examples tests/programs

      - name: Generated docs are up to date
        run: node tools/gen-docs.ts --check

      - name: Conformance suite is up to date
        run: node tools/gen-conformance.ts --check

      - name: Brand images are committed
        run: node tools/render-assets.ts --check

      - name: Diagnostics read plainly in CI
        run: node src/cli/index.ts --no-baa check examples

  cli:
    name: cli smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - run: npm ci

      - name: A new project works end to end
        run: |
          node src/cli/index.ts --version
          node src/cli/index.ts doctor
          mkdir -p /tmp/flock && cd /tmp/flock
          node "$GITHUB_WORKSPACE/src/cli/index.ts" init . --name ci_flock
          node "$GITHUB_WORKSPACE/src/cli/index.ts" run
          node "$GITHUB_WORKSPACE/src/cli/index.ts" test
          node "$GITHUB_WORKSPACE/src/cli/index.ts" build
          node "$GITHUB_WORKSPACE/src/cli/index.ts" fmt --check .

      # The published package is a different artefact from the checkout, and
      # only this step exercises it. 0.2.0 shipped `.ts` sources with `bin`
      # pointing at one, installed cleanly, and then failed on first run:
      # Node refuses to strip types under node_modules. Everything above this
      # passed for that release, because it all runs from the checkout.
      - name: The tarball works when installed
        run: |
          npm pack
          mkdir -p /tmp/consumer && cd /tmp/consumer
          npm init -y > /dev/null
          npm install "$GITHUB_WORKSPACE"/baa-lang-*.tgz
          {
            echo 'baa "installed"'
            echo 'baa 2 + 3'
          } > check.baa
          ./node_modules/.bin/baa version
          ./node_modules/.bin/baa run check.baa | tee out.txt
          grep -qx 'installed' out.txt
          grep -qx '5' out.txt
